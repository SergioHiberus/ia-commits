export PATH="/usr/bin:$PATH"

# Path to the commit message file
COMMIT_MSG_FILE=$1

# Execute the PHP verification script
php ./scripts/verify-commit.php "$COMMIT_MSG_FILE"

# The PHP script exit code determines if the commit is accepted (0) or aborted (any other number)